services:
  - type: web
    name: niuniu-auto-order
    runtime: docker          # 关键修改：从 image 改为 docker
    repo: https://github.com/你的用户名/你的仓库名   # 替换成你的真实仓库地址
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.13