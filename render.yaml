services:
  - type: web
    name: niuniu-auto-order   # 你可以改成任意名字
    runtime: image
    repo: https://github.com/你的用户名/你的仓库名  # 替换成你的仓库地址
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.13